# letterboxd-for-restaurants


backend: pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
(inside \backend)


frontend: npm install
npm run dev

(App at http://localhost:3000)
(inside \frontend)
